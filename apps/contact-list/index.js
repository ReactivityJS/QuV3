/**
 * CONTACT LIST — server-side half. UI-only (see client.js) - see apps/app-list/index.js for why this stays a documented no-op.
 */
export async function register(qu, manifest) {
  console.log(`[contact-list] registered (${manifest.name}@${manifest.version}) - UI-only, see client.js`);
}
