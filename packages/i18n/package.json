{
  "name": "@qu/i18n",
  "version": "0.1.0",
  "description": "Minimal, dependency-free i18n: a locale-keyed dictionary plus t(key, params). Not a full ICU/pluralization engine - just enough structure that every app's user-facing strings live in one lookup table per locale from day one, instead of a retrofit later. No global singleton: every app creates its own t() from its own dictionary, so this stays genuinely optional infrastructure.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "license": "MIT"
}
